version: '3.8'

services:
  vk_uploader:
    build: .
    environment:
      - VK_TOKEN=your_vk_token
      - GROUP_ID=your_group_id
      - PROXY_URL=http://proxy:port
    volumes:
      - ./clips:/app/clips
